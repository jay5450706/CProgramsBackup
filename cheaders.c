//#include <stdio.h>
#include "math_functions.h"

int main(){
    int x = 5, y = 3;
    printf("Sum is: %d\n", add(x, y));
    printf("Difference is: %d", subtract(x, y));
    printf("\nVar is: %d", var);
    return 0;
}